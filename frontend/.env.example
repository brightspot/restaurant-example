# Local Development Overrides
# Copy this to .env to override default settings
# .env is gitignored and won't be committed

# Override service type for local testing
VITE_SERVICE_TYPE=mock
VITE_FORCE_GLOBAL_SERVICE_TYPE=true
